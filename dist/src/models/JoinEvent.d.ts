export default class User {
    private _idEvent;
    private _idUser;
    private _idType;
    constructor(idEvent: number, idUser: number, idType: number);
    idEvent: number;
    idUser: number;
    idType: number;
}
