export default class User {
    private _id;
    private _username;
    private _email;
    private _password;
    constructor(username: string, email: string, password: string);
    id: number;
    username: string;
    email: string;
    password: string;
}
