export default class Uses {
    private _idEvent;
    private _idQuestionnaire;
    private _idOption;
    constructor(idEvent: number, idQuestionnaire: number, idOption: number);
    idEvent: number;
    idQuestionnaire: number;
    idOption: number;
}
